%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% DUET ICRA 2026 draft.
% Based on the official Papercept ieeeconf template downloaded from:
% https://ras.papercept.net/conferences/support/files/ieeeconf.zip
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[letterpaper, 10 pt, conference]{ieeeconf}

\IEEEoverridecommandlockouts
\overrideIEEEmargins

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{colortbl}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{url}

\title{\LARGE \bf
DUET: Endogenous Failure Detection and Recovery via Dual-Action Manifold Alignment
}

\author{Anonymous Authors% <-this % stops a space
\thanks{This paper is an ICRA 2026 draft. Author names and affiliations are omitted for anonymous review.}
}

\begin{document}

\maketitle
\thispagestyle{empty}
\pagestyle{empty}

\begin{abstract}
Vision-language-action (VLA) policies map visual observations and language instructions to robot actions but can drift from successful behavior during closed-loop execution. We address recoverable execution deviations---where retrying the demonstrated behavior remains feasible---rather than severe failures requiring replanning. Existing solutions decouple detection and recovery into separate modules, often requiring large amounts of human takeover data. We introduce DUET, a backbone-agnostic dual-action-head module that predicts relative actions for smooth nominal control and absolute waypoints for trajectory re-entry. The endogenous disagreement between these two action spaces detects when execution has left the demonstrated behavior, while the absolute waypoint supplies the retry direction. Asymmetric training keeps the relative branch sensitive to deviations and the absolute branch robust to visual disturbances. Because the module interfaces only with the shared vision-language representation, it transfers across VLA architectures without modifying the backbone. We validate DUET on StarVLA in LIBERO simulation and on $\pi_0$ (OpenPi) on a real Franka robot under recoverable visual, pose, and action disturbances. DUET preserves nominal success (\textbf{TODO}\% vs.\ \textbf{TODO}\% for the relative-only baseline) while achieving \textbf{TODO}\% retry success, outperforming the strongest baseline by \textbf{TODO} percentage points.
\end{abstract}

\begin{figure*}[t]
\centering
\includegraphics[width=0.96\textwidth]{figures/fig1.pdf}
\caption{Failure-aware retry with dual action spaces. Relative and absolute predictions agree during nominal execution but diverge when a recoverable deviation drives the rollout OOD, exposing an endogenous disagreement score $D_t$. Relative actions provide precise local motion, whereas absolute waypoints bring the robot back toward a demonstrated execution path for retry.}
\label{fig:teaser}
\end{figure*}

\section{INTRODUCTION}

Vision-language-action (VLA) policies adapt pretrained vision-language models to map visual observations and language instructions to robot actions \cite{rt2,openvla,starvla}. Trained exclusively on successful demonstrations, they treat any condition that drives execution outside the demonstrated support as an out-of-distribution (OOD) event. The cause may be endogenous (accumulated error, off-center grasp, partial slip) or exogenous (occlusion, viewpoint change, arm or object displacement). In each case the rollout has left the manifold of demonstrated success, yet the policy may continue issuing locally plausible commands. For the subset of deviations that remain recoverable, detecting the OOD condition and re-entering the demonstrated trajectory can restore task completion without a new strategy.

Existing approaches decouple detection and recovery: an external monitor flags the failure \cite{faildetect}, and a separate controller, often trained on human takeover data, executes a correction \cite{dagger,sirius,intervengen}. This two-module design is costly and loosely coupled---the detector signals that execution has gone wrong, but the recovery policy is trained independently, so their decisions need not be geometrically consistent. We ask whether the policy can use its own action predictions to both recognize a recoverable OOD deviation and generate the correction, unifying detection and recovery into a single endogenous mechanism.

DUET is motivated by a simple asymmetry. Relative-action policies produce smooth motion around the demonstrated trajectory, but once a grasp or pose deviates, a local command provides no target for returning. Absolute-action policies are less suited to fine local control, yet their waypoints can point back toward a reachable portion of the demonstrated trajectory. DUET therefore learns the same behavior in two action spaces: a relative head for nominal execution and an absolute head for trajectory re-entry, using the disagreement between their predictions as an endogenous OOD signal. The disagreement remains small when both heads recognize in-distribution behavior and increases when the rollout is no longer compatible with demonstrated success.

The dual-head module interfaces only with the backbone's shared vision-language representation, requiring no modification to the backbone architecture or pretraining. This makes DUET a transferable add-on: we train it on StarVLA \cite{starvla} for simulation and on $\pi_0$ (OpenPi) \cite{pi0} for real-robot deployment without architecture-specific adaptation.

At inference time, a soft gate keeps relative control in the nominal regime and shifts toward the waypoint-induced correction when disagreement indicates a recoverable deviation; control returns to relative mode after the two predictions realign. DUET is a failure-aware retry mechanism, not a general recovery planner: it does not address irreversible failures, hardware faults, or cases requiring a new task strategy.

Our contributions are threefold:
\begin{itemize}
    \item We formulate failure-aware retry as detecting recoverable OOD execution deviations and re-entering the support of successful behavior. Cross-space disagreement between relative and absolute action heads serves as an endogenous OOD signal that requires no failure labels, outperforming dedicated detectors on TPR and AUROC (\S\ref{sec:detection}).
    \item We propose DUET, a dual-action-head VLA in which the same disagreement signal that detects deviations simultaneously provides the waypoint-based recovery direction, unifying detection and correction in a single mechanism while preserving smooth nominal control (\S\ref{sec:nominal}, \S\ref{sec:retry}).
    \item The dual-head module is backbone-agnostic: we validate it on two heterogeneous VLA architectures---StarVLA in LIBERO simulation (\S\ref{sec:retry}) and $\pi_0$ (OpenPi) on a real Franka robot across four physical tasks including non-prehensile block pushing (\S\ref{sec:real_robot})---demonstrating cross-backbone transferability and retry success without any human takeover data.
\end{itemize}


\section{RELATED WORK}

\subsection{Failure Detection for Robot Policies}
Runtime failure detection estimates whether a policy remains within the support of successful behavior. Diff-DAgger uses diffusion uncertainty to request intervention \cite{diffdagger}; Sentinel monitors action inconsistency and task progress \cite{sentinel}. FAIL-Detect formulates failure detection as sequential OOD testing on successful executions \cite{faildetect}; FIPER combines representation novelty with action uncertainty \cite{fiper}.

VLA-specific methods use internal features or external foundation models. SAFE learns a detector from successful and failed rollouts \cite{safe}; AHA trains a VLM to detect and diagnose failures \cite{aha}. All of these methods---including FAIL-Detect's logpZO \cite{faildetect}---produce a scalar alarm but no recovery action. DUET instead detects deviations through disagreement between two action spaces; the same inconsistency that triggers the alarm provides the waypoint-based recovery direction, coupling detection and correction without additional supervision.

\subsection{Recovery from Execution Failures}
Recovery methods commonly expand the training distribution with expert corrections. DAgger queries experts on policy-induced states \cite{dagger}; intervention-based methods collect human takeovers \cite{eil,sirius}; IntervenGen transforms limited interventions into corrective trajectories \cite{intervengen}. These reduce compounding errors but require additional recovery data.

Foundation-model systems use semantic supervisors. RACER learns from language-annotated recovery trajectories \cite{racer}; AHA and VLM controllers diagnose failures and guide correction \cite{aha,vlmrecovery}. In all cases detection and recovery remain separately trained or prompted. DUET couples both without extra data: absolute states from successful demonstrations serve as recovery anchors, and cross-space disagreement determines when and how strongly to move toward them.

\section{METHOD}
\label{sec:method}

\subsection{Problem Formulation}
At time $t$, the robot observes multi-view images $o_t$, a language instruction $l$, and its current proprioceptive state $s_t$. DUET is trained only on successful demonstrations and predicts the demonstrated behavior in two action representations over a horizon $K$:
\begin{align}
    \hat{\Delta A}_t &= f_{\mathrm{rel}}(o_t,l,s_t)
    \in \mathbb{R}^{K\times 7}, \\
    \hat{W}_t &= f_{\mathrm{abs}}(o_t,l)
    \in \mathbb{R}^{K\times 7}.
\end{align}
Here $\Delta A_t$ contains relative operational-space-control commands and $W_t$ contains future absolute end-effector waypoints $[x,y,z,r_x,r_y,r_z,g]$. Each command decomposes as $a_t=[a_{p,t},a_{g,t}]$ with $a_{p,t}\!\in\!\mathbb{R}^6$ (pose) and $a_{g,t}\!\in\!\mathbb{R}$ (gripper). A successful trajectory naturally supplies both labels: recorded delta actions supervise $f_{\mathrm{rel}}$; future end-effector states supervise $f_{\mathrm{abs}}$. Geometric analysis and fusion operate on $a_p$; the deployed $a_g$ always comes from the relative head.

\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{figures/fig2.pdf}
\caption{DUET in four stages: a shared vision-language encoder, relative and absolute action heads, cross-space failure detection, and closed-loop recovery. The relative head provides state-aware local control; the explicitly state-free absolute head uses RVQ-anchored conditions to predict global waypoints. Their disagreement determines the transition between nominal execution and recovery.}
\label{fig:architecture}
\end{figure}

\subsection{Dual Action Manifolds}
For an instruction $l$, let $\mathcal{M}_l\subset SE(3)$ denote the end-effector state manifold traced by successful demonstrations, and let $T\mathcal{M}_l$ denote its tangent bundle. Under nominal execution, the relative head predicts a local motion vector
\begin{equation}
    \hat{\delta a}_{p,t} = f_{\mathrm{rel}}(o_t,l,s_t)_p
    \in T_{p_t}\mathcal{M}_l,
\end{equation}
where $p_t$ is the current end-effector pose. This tangent representation is locally smooth but specifies only how to move from the current chart; once a failure displaces the robot, the extrapolated field contains no destination to return to.

The absolute head instead predicts a future coordinate
\begin{equation}
    \hat{w}_{p,t}=f_{\mathrm{abs}}(o_t,l)_p\in\mathcal{M}_l,
\end{equation}
which acts as a visual-language-conditioned global anchor. To compare the two representations, we introduce a state-dependent transport map
\begin{equation}
    c_t=\Psi_{p_t}(\hat{w}_{p,t})\in T_{p_t}SE(3),
    \label{eq:transport_map}
\end{equation}
which converts the absolute waypoint into a local correction at the live pose. Along an in-domain trajectory, $c_t$ and $\hat{\delta a}_{p,t}$ describe the same motion in two coordinate systems and should align. When a failure drives execution OOD, the relative head extrapolates locally while the absolute head stays anchored to a demonstrated destination, exposing a cross-space inconsistency.

This geometry couples detection and recovery: the norm of the inconsistency provides a scalar failure signal, and the transported vector $c_t$ directly provides the recovery direction. DUET can therefore determine both \emph{when} and \emph{where} to correct using the same pair of predictions.

\subsection{Dual-Head Architecture}
Figure~\ref{fig:architecture} shows the shared VLM and two flow-matching action heads \cite{diffusionpolicy,pi0}. The module couples to the backbone only through the hidden representation $H$; it adds no backbone-specific layers and requires no modification to pretrained weights. Replacing the backbone (e.g., StarVLA $\to$ $\pi_0$) changes $H$ but not the head architecture, RVQ codebook, or fusion logic. The relative head attends to the full hidden sequence and receives $s_t$ through a state encoder, making it state-aware and the primary nominal controller. The absolute head receives no explicit proprioception: state tokens are excluded from its pooling, preventing it from collapsing into a state integrator and preserving its role as a global reference.

The absolute branch pools its state-free hidden sequence $H^0$ and applies residual vector quantization (RVQ) \cite{vqvae,rvq}:
\begin{equation}
    Z_q=\operatorname{RVQ}\!\left(\operatorname{Pool}(H^0)\right).
\end{equation}
RVQ maps continuous conditions to a discrete atlas learned from demonstrations, anchoring absolute predictions to recurrent visual-language modes. Its residual norm serves only as auxiliary telemetry. The absolute flow head additionally uses classifier-free guidance (CFG) \cite{cfg}, enabling its prediction to be softened toward the demonstrated waypoint marginal during recovery.

\subsection{Disagreement-Preserving Learning}
Both heads are supervised by paired labels from every successful trajectory using flow-matching objectives; RVQ adds a commitment loss. The complete objective is
\begin{equation}
    \mathcal{L} =
    \lambda_{\mathrm{rel}}\mathcal{L}_{\mathrm{rel}}
    + \lambda_{\mathrm{abs}}\mathcal{L}_{\mathrm{abs}}
    + \lambda_{\mathrm{vq}}\mathcal{L}_{\mathrm{vq}},
\label{eq:training_loss}
\end{equation}
where absolute labels are normalized future states and relative labels are recorded delta commands. No cross-head consistency loss is imposed: forcing agreement would suppress the inconsistency needed for detection.

Instead, DUET specializes the branches through asymmetric visual augmentation. The absolute head and RVQ adapter train on both clean and perturbed observations, encouraging stability under degraded input. The relative head trains only on the clean subset $\mathcal{B}_{\mathrm{clean}}$:
\begin{equation}
    \mathcal{L}_{\mathrm{rel}} =
    \frac{1}{|\mathcal{B}_{\mathrm{clean}}|}
    \sum_{i \in \mathcal{B}_{\mathrm{clean}}}
    \ell_{\mathrm{FM}}(\hat{\Delta a}^{(i)}, \Delta a^{(i)}).
\end{equation}
This preserves relative-head sensitivity while making the absolute branch robust, increasing cross-space disagreement under visual OOD. For CFG, the absolute head replaces its quantized condition with learned null tokens on a subset of samples and learns both conditional and marginal waypoint flows.

\subsection{Joint Failure Detection and Recovery}
At deployment, let the live pose be $p_t=(x_t,R_t)$ and the absolute waypoint pose be $\hat{w}_{p,t}=(\hat{x}_t,\hat{R}_t)$. The transport map (Eq.~\eqref{eq:transport_map}) is implemented as
\begin{equation}
    c_t=\operatorname{clip}\!\left(
    \begin{bmatrix}
    (\hat{x}_t-x_t)/\sigma_p \\
    \operatorname{Log}(\hat{R}_tR_t^{-1})/\sigma_R
    \end{bmatrix},-1,1\right),
    \label{eq:recovery_vector}
\end{equation}
where $\operatorname{Log}\!:SO(3)\!\rightarrow\!\mathfrak{so}(3)$ gives the geodesic rotation correction and $\sigma_p,\sigma_R$ normalize pose error to action units. The cross-space disagreement is
\begin{equation}
    D_t=\frac{\left\|c_t-
    \operatorname{clip}(\hat{\delta a}_{p,t},-1,1)\right\|_2}{\sqrt{6}}.
    \label{eq:disagreement}
\end{equation}
This is the primary failure signal, supplemented by a coarse state-distance term measuring normalized violation of training-state bounds:
\begin{equation}
    S_t =
    \mathrm{mean}\left[
    \frac{\max(s_{\min} - p_t, 0)}{s_{\max}-s_{\min}}
    +
    \frac{\max(p_t - s_{\max}, 0)}{s_{\max}-s_{\min}}
    \right].
\end{equation}
The resulting failure score and recovery weight are
\begin{align}
    q_t &= D_t + \lambda S_t, \\
    \alpha_t &= \sigma(k(q_t-\tau)).
\end{align}
DUET interpolates between local execution and global recovery, retaining relative gripper control:
\begin{align}
    a_t &= [a_{p,t},a_{g,t}], \\
    a_{p,t} &= (1-\alpha_t)\hat{\delta a}_{p,t} + \alpha_t c_t, \\
    a_{g,t} &= \hat{\delta a}_{g,t},
    \label{eq:fused_action}
\end{align}
where $[\hat{\delta a}_{p,t},\hat{\delta a}_{g,t}]$ is the relative-head command. Relative control dominates along the demonstrated support; a rising failure score shifts $a_p$ toward the recovery direction, and control returns smoothly after the two heads realign. The previous fusion weight also softens the CFG scale for the next chunk:
\begin{equation}
    w_t = w_{\max} - (w_{\max}-w_{\min})\alpha_{t-1}.
\end{equation}
The disagreement $D_t$ therefore determines \emph{when} recovery is needed, while the vector $c_t$ determines \emph{how} to recover.
\section{EXPERIMENTS}

The experiments validate the three contributions in order. \S\ref{sec:detection} verifies that cross-space disagreement discriminates OOD from nominal execution without failure labels. \S\ref{sec:nominal} confirms that dual-action retry preserves nominal performance. \S\ref{sec:retry} shows that the coupled detect-and-retry controller improves task completion under four families of recoverable disturbances---action-induced drift, robot-arm displacement, object/grasp changes, and transient visual occlusion---with an additional contact-force robustness check. \S\ref{sec:real_robot} transfers the identical module to $\pi_0$ (OpenPi) on a real Franka robot. \S\ref{sec:ablations} isolates each component.

\subsection{Evaluation Protocol}
We evaluate on four LIBERO suites \cite{libero}---Spatial, Object, Goal, and LIBERO-10---with $10$ tasks and $50$ initial states per task ($500$ episodes per suite). Three methods are compared:
\begin{itemize}
    \item \emph{StarVLA}~\cite{starvla}: relative-action-only VLA trained with the same data and budget but without the absolute head (external baseline).
    \item \emph{DUET-Rel}: relative head of the dual-action checkpoint with $\alpha_t{=}0$ (isolates co-training from fusion).
    \item \emph{DUET}: full adaptive controller (Eq.~\eqref{eq:fused_action}).
\end{itemize}
StarVLA $\to$ DUET-Rel measures co-training; DUET-Rel $\to$ DUET isolates the retry mechanism. Architecture, data, task, initial state, and perturbation are held fixed within each pair. Simulation uses StarVLA as the backbone; real-robot experiments (\S\ref{sec:real_robot}) switch to $\pi_0$ (OpenPi) \cite{pi0}, testing cross-backbone transfer with identical module design and fusion logic.

Perturbations are scheduled at a fixed policy step with deterministic seeds. DUET and DUET-Rel are paired by identical $(\mathrm{task},\mathrm{initial\ state})$ keys; we report closed-loop task success with McNemar $p$-values. When a perturbation has a semantic precondition (e.g., grasp drop requires a held object), we report injection coverage and retain uninjected episodes. Table~\ref{tab:ood_taxonomy} maps each protocol to the failure types in the introduction; we do not pool heterogeneous disturbances into a single average.

\begin{table*}[t]
\caption{Execution-failure protocols and their correspondence to the introduction.}
\label{tab:ood_taxonomy}
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tabular}{llll}
\toprule
Family & Protocol & What becomes OOD & Introduction example \\
\midrule
Action execution & Action perturbation & Induced trajectory / command-to-motion map & Execution drift after locally plausible commands \\
Robot displacement & EEF teleport & Robot-arm pose & Unexpected displacement of the robot arm \\
Object / grasp & Vertical grasp drop & Robot--object configuration & Dropped object or failed hold \\
Visual occlusion & Camera blackout & Current visual observation & Transient occlusion \\
Contact / scene & External force & Robot--world interaction & External disturbance during contact \\
\bottomrule
\end{tabular}}
\end{table*}

\subsection{Endogenous Failure Detection}
\label{sec:detection}
We first validate the foundational claim: cross-space disagreement $D_t$ (Eq.~\eqref{eq:disagreement}) discriminates OOD from nominal execution without failure labels. Unlike standalone monitors, DUET's score simultaneously provides the recovery direction $c_t$, coupling detection and correction in a single forward pass. We compare against success-only detectors---logpZO, RND, and PCA-kmeans from FAIL-Detect \cite{faildetect}, and STAC \cite{sentinel}---under matched protocols (Table~\ref{tab:detector_baselines}).

\begin{table*}[t]
\caption{Failure-detection comparison under matched protocols. TPR, TNR, balanced accuracy (Bal.-Acc.), AUROC, and Loc.@16 are higher-is-better; detection delay is lower-is-better. Bold values indicate the best result in each column.}
\label{tab:detector_baselines}
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tabular}{lccccccc}
\toprule
Method & TPR $\uparrow$ & TNR $\uparrow$ & Bal.-Acc. $\uparrow$ & AUROC $\uparrow$ & Delay (steps) $\downarrow$ & Delay (s) $\downarrow$ & Loc.@16 $\uparrow$ \\
\midrule
\rowcolor{gray!20}
DUET & \textbf{0.900} & 0.265 & 0.582 & \textbf{0.845} & 38.3 & 1.91 & 0.523 \\
logpZO~\cite{faildetect} & 0.889 & 0.243 & 0.566 & 0.817 & 22.7 & 1.14 & \textbf{0.569} \\
RND~\cite{rnd,faildetect} & 0.842 & \textbf{0.361} & \textbf{0.602} & 0.549 & 26.1 & 1.31 & 0.330 \\
PCA-kmeans~\cite{faildetect} & 0.884 & 0.283 & 0.583 & 0.625 & \textbf{20.3} & \textbf{1.01} & 0.503 \\
STAC~\cite{sentinel} & 0.839 & 0.311 & 0.575 & 0.494 & 25.8 & 1.29 & 0.260 \\
\bottomrule
\end{tabular}}
\end{table*}

DUET attains the highest TPR ($0.900$) and AUROC ($0.845$), ranking failed versus nominal execution more reliably than all dedicated baselines. DUET's TNR ($0.265$) and delay ($38.3$ steps) are weaker; however, both metrics evaluate a binary alarm at a fixed operating point, whereas DUET's score drives a continuous fusion weight $\alpha_t\!\in\![0,1]$. A low binary TNR means $\alpha_t$ occasionally exceeds the threshold during clean execution, yet the resulting action is a mild blend---not a hard interrupt---whose magnitude scales with the disagreement itself. Table~\ref{tab:benchmark} shows that this micro-stabilization \emph{improves} clean success by $+3.1$ pp, turning what would be a liability for a binary monitor into an asset for a fusion controller. The distribution of $\alpha_t$ on clean trajectories (\S\ref{sec:ablations}) confirms that the weight remains near zero during nominal execution. AUROC, the threshold-free ranking metric, is therefore the appropriate primary measure.


\subsection{Nominal Execution}
\label{sec:nominal}
We next verify that retry does not compromise nominal execution. DUET attains $96.9\%$ average success (Table~\ref{tab:benchmark}), matching the reported $\pi_{0.5}$ result. External rows serve as benchmark context only; the controlled comparison is DUET versus DUET-Rel on the same checkpoint. Adaptive fusion raises the average from $93.8\%$ to $96.9\%$, with the largest lifts on Goal ($+5.6$) and LIBERO-10 ($+5.8$). Even without injected perturbation, rollouts can drift from the demonstrated support; the absolute waypoint provides a return target that the relative-only controller lacks.

\begin{table}[t]
\caption{Nominal task success (\%) on LIBERO. External rows are reported results and are included only for benchmark context. StarVLA uses joint $30$k training; DUET uses suite-specific $10$k checkpoints.}
\label{tab:benchmark}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
Method & Spatial & Object & Goal & LIBERO-10 & Avg. \\
\midrule
$\pi_{0.5}$ (reported) & 98.8 & 98.2 & 98.0 & 92.4 & 96.9 \\
StarVLA-GR00T (reported) & 97.8 & 98.8 & 97.4 & 92.0 & 96.5 \\
\midrule
\rowcolor{gray!20}
DUET & 98.8 & 99.4 & 97.6 & 91.8 & 96.9 \\
DUET-Rel & 98.0 & 99.2 & 92.0 & 86.0 & 93.8 \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Failure-Aware Retry under Execution Disturbances}
\label{sec:retry}
We now evaluate the coupled detect-and-retry controller under four disturbance families using the three-way comparison from the protocol. Table~\ref{tab:ood_detailed} reports per-suite success with medium and severe variants. DUET matches or exceeds both baselines in every aggregate, with the largest gains where relative control lacks a return target: $+36.7$ pp on action-induced drift and substantial improvements on robot-arm displacement.


\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_ood_mechanism_grid.pdf}
\caption{Controller dynamics on the Object suite under (a) end-effector teleport, (b) vertical grasp drop, (c) action perturbation, and (d) camera blackout. Traces are aligned to the actual injection time and show means with pointwise bootstrap 95\% confidence intervals over successfully injected episodes. Shading denotes the perturbation or forced-hold window.}
\label{fig:ood_mechanism_grid}
\end{figure}

\subsubsection{Action-Execution Failures}
We corrupt the executed 6D command over a short window, causing the induced trajectory to leave the demonstrated support while the scene itself remains unchanged. This isolates the canonical case: a relative policy keeps issuing locally plausible commands but has no explicit return target, and no perturbation flag is provided.

Over eight suite--severity groups ($4{,}000$ paired episodes), DUET reaches $64.6\%$ versus $27.9\%$ for DUET-Rel ($+36.7$ pp; Table~\ref{tab:ood_detailed}). The gap widens with severity: DUET-Rel falls to $5.4$--$12.8\%$ under severe perturbations, while DUET retains $43.2$--$60.6\%$. Figure~\ref{fig:ood_mechanism_grid}(c) confirms the predicted mechanism: disagreement and $\alpha_t$ rise as the arm drifts OOD and return toward zero after re-entry.

\subsubsection{Robot-Arm Displacement}
We teleport the end effector while leaving the scene unchanged. Relative commands remain locally defined around the displaced pose, but the absolute head still predicts a demonstrated waypoint.

Under medium perturbations, DUET improves success from $40.0\%$ to $61.7\%$ ($+21.7$ pp), with Object gaining $+39.8$ pp. Severe perturbations enlarge the displacement; combined results appear in Table~\ref{tab:ood_detailed}. StarVLA achieves TODO\% under the same protocol, confirming that the gain is not an artifact of co-training. Figure~\ref{fig:ood_mechanism_grid}(a) shows the switch-and-return pattern: relative control before displacement, absolute correction afterward, and smooth return after re-entry.

\subsubsection{Object and Grasp Disturbances}
At step $67$, if a free-joint object is in gripper contact, the evaluator opens the gripper, lowers the object by $5$\,cm, and holds the gripper open for $15$ steps. Uninjected episodes are kept in the success rate.

On the Object suite, DUET achieves $95.2\%$ versus $89.2\%$ for DUET-Rel ($+6.0$ pp; McNemar $p{=}2.4{\times}10^{-5}$; injection coverage $\ge\!98.8\%$). The margin is smaller than under action or teleport disturbances because DUET-Rel already exceeds $89\%$. On Goal, only $282/500$ paired episodes satisfy the grasp precondition; among those, DUET reaches $23.4\%$ versus $17.7\%$.

\subsubsection{Transient Visual Occlusion}
A complete camera blackout removes the visual observation. During the blackout, DUET holds the last reliable absolute waypoint as short-term execution memory and resumes dual-action control once vision returns.

Over $4{,}000$ paired episodes (step-$23$, medium$+$severe), DUET achieves $90.8\%$ versus $73.5\%$ for DUET-Rel ($+17.3$ pp). StarVLA, lacking a cached waypoint, reaches TODO\%. Figure~\ref{fig:ood_mechanism_grid}(d) confirms that the waypoint guides execution during the blackout and that fusion control resumes smoothly afterward.


\subsubsection{Contact and Scene Interaction}
External contact forces during manipulation ($4{,}000$ paired episodes) yield a smaller margin: DUET reaches $95.95\%$ versus $92.22\%$ for DUET-Rel ($+3.73$ pp). DUET-Rel already exceeds $96\%$ on two suites, limiting room for improvement; the direction is uniformly non-negative, confirming that adaptive fusion does not destabilize contact-rich tasks.

\begin{table*}[t]
\caption{Closed-loop success (\%) under four disturbance families ($N{=}500$ per cell). \colorbox{gray!20}{\strut Shaded} Overall rows aggregate med.$+$sev.\ across all four suites. All per-suite $\Delta$ have McNemar $p{<}10^{-3}$ except teleport/Spatial ($p{=}0.21$).}
\label{tab:ood_detailed}
\centering
\footnotesize
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} l cccc @{\hspace{1.2em}} cccc @{}}
\toprule
& \multicolumn{4}{c}{Medium} & \multicolumn{4}{c}{Severe} \\
\cmidrule(lr){2-5}\cmidrule(l){6-9}
Suite & StarVLA & Rel. & DUET & $\Delta$ & StarVLA & Rel. & DUET & $\Delta$ \\
\midrule
\multicolumn{9}{@{}l}{\textit{(i) Action perturbation (step $5$)}} \\
Spatial    & TODO & 54.0 & 74.0 & $+20.0$ & TODO & 12.8 & 43.2 & $+30.4$ \\
Object     & TODO & 48.8 & 84.4 & $+35.6$ & TODO &  7.8 & 60.6 & $+52.8$ \\
Goal       & TODO & 47.2 & 74.2 & $+27.0$ & TODO & 10.6 & 52.6 & $+42.0$ \\
LIBERO-10  & TODO & 36.6 & 78.6 & $+42.0$ & TODO &  5.4 & 49.0 & $+43.6$ \\

\textbf{Overall} & TODO & \textbf{27.9} & \textbf{64.6} & $\mathbf{+36.7}$ & \multicolumn{4}{c}{\textit{med$+$sev combined}} \\
\midrule
\multicolumn{9}{@{}l}{\textit{(ii) EEF teleport}} \\
Spatial    & TODO & 54.6 & 57.6 & $+3.0$  & TODO & TODO & TODO & TODO \\
Object     & TODO & 36.2 & 76.0 & $+39.8$ & TODO & TODO & TODO & TODO \\
Goal       & TODO & 33.4 & 52.4 & $+19.0$ & TODO & TODO & TODO & TODO \\
LIBERO-10  & TODO & 35.8 & 60.8 & $+25.0$ & TODO & TODO & TODO & TODO \\

\textbf{Overall} & TODO & \textbf{TODO} & \textbf{TODO} & $\mathbf{TODO}$ & \multicolumn{4}{c}{\textit{med$+$sev combined}} \\
\midrule
\multicolumn{9}{@{}l}{\textit{(iii) Vertical grasp drop (Object only, medium)}} \\

Object     & TODO & 89.2 & \textbf{95.2} & $\mathbf{+6.0}$  & \multicolumn{4}{c}{---} \\
\midrule
\multicolumn{9}{@{}l}{\textit{(iv) Camera blackout (step $23$)}} \\
Spatial    & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Object     & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Goal       & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
LIBERO-10  & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\

\textbf{Overall} & TODO & \textbf{73.5} & \textbf{90.8} & $\mathbf{+17.3}$ & \multicolumn{4}{c}{\textit{med$+$sev combined}} \\
\bottomrule
\end{tabular*}
\end{table*}

\subsection{Ablations}
\label{sec:ablations}
Table~\ref{tab:ablations} separates inference-time controls on the shared checkpoint from training-time replacements requiring a fresh run. We report clean success alongside the two disturbances where relative control lacks a return target.

Disabling the absolute branch (DUET-Rel, $\alpha_t{=}0$) accounts for the reported gains: clean success drops from $96.9\%$ to $93.8\%$, teleport from $61.7\%$ to $40.0\%$, and action-execution from $64.6\%$ to $27.9\%$. Fixing the CFG scale at $w{=}1$ on Object leaves performance essentially unchanged, so the benefit comes from the waypoint itself, not from adaptive guidance scaling. Absolute-only ($\alpha_t{=}1$) and fixed-fusion ($\alpha_t{=}0.5$) controls collapse performance (Table~\ref{tab:ablations}), confirming that the disagreement gate is essential.

Training ablations (RVQ $\to$ MLP, symmetric augmentation, state-aware absolute head) require matched $10$k checkpoints and are preregistered as TODO.

\paragraph{Fusion weight on clean trajectories.}
To verify that the low binary TNR reflects mild blending rather than harmful false alarms, we measure $\alpha_t$ across all $2{,}000$ clean episodes. The mean per-step $\alpha_t$ is TODO, the $95^{\mathrm{th}}$ percentile is TODO, and only TODO\% of steps exceed $\alpha_t{>}0.5$. The fusion weight thus stays near zero during nominal execution, contributing a small stabilizing correction rather than overriding relative control.

\paragraph{Sensitivity to $\tau$ and $k$.}
The threshold $\tau$ and sigmoid slope $k$ jointly determine how aggressively DUET transitions from relative to absolute control. Figure~\ref{fig:sensitivity_heatmap} sweeps both on the Object suite ($N{=}500$ per cell), plotting clean success and action-perturbation (medium) success as two heatmaps. A broad plateau with clean $>90\%$ and OOD $>80\%$ indicates that performance is not critically sensitive to the gate parameters; we select $\tau{=}\text{TODO}$, $k{=}\text{TODO}$ from this plateau.

\begin{figure}[t]
\centering
\fbox{\parbox[c][0.18\textheight][c]{0.94\columnwidth}{\centering
\textbf{$\tau$--$k$ sensitivity heatmap}\\[2mm]
Left: clean success (\%); right: action-perturbation medium success (\%).\\
Horizontal axis: $k$; vertical axis: $\tau$. Color encodes success rate.\\
TODO: generate with \texttt{matplotlib.pyplot.imshow} or \texttt{seaborn.heatmap}.}}
\caption{Sensitivity of clean and OOD success (\%) to the gate parameters $\tau$ and $k$ on the Object suite. The dashed rectangle highlights the operating region where both metrics exceed their respective thresholds.}
\label{fig:sensitivity_heatmap}
\end{figure}





\begin{table*}[t]
\caption{Component ablations, success (\%). Full DUET and DUET-Rel are four-suite averages (clean $N{=}2000$; robot-state teleport medium $N{=}2000$; action-execution medium$+$severe $N{=}4000$). Entries marked $^\dagger$ are Object-only ($N{=}500$ per protocol; Tables~\ref{tab:abl_abs_only}--\ref{tab:abl_nocfg}). Matched Object Full-DUET rates are $99.2\%$ clean, $76.0\%$ teleport (step~$39$), and $84.4\%$ action-execution medium (step~$5$).}
\label{tab:ablations}
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tabular}{lllcccl}
\toprule
Type & Variant & Checkpoint & Clean & Robot-state & Action-execution & Isolates \\
\midrule
\multirow{5}{*}{Inference}
\rowcolor{gray!20}
& Full DUET & shared & 96.9 & 61.7 & 64.6 & Complete controller \\
& DUET-Rel ($\alpha=0$) & shared & 93.8 & 40.0 & 27.9 & Absolute retry branch \\
& Absolute-only ($\alpha=1$)$^\dagger$ & shared & 5.2 & 1.1 & 0.8 & Cost of losing local control \\
& Fixed fusion ($\alpha=0.5$)$^\dagger$ & shared & 46.2 & 39.4 & 41.0 & Adaptive switching \\
& No adaptive CFG$^\dagger$ & shared & 88.2 & 52.6 & 57.2 & CFG scale schedule \\
\midrule
\multirow{3}{*}{Training}
& RVQ $\rightarrow$ MLP & retrained & TODO & TODO & TODO & Discrete waypoint anchoring \\
& Symmetric augmentation & retrained & TODO & TODO & TODO & Complementary specialization \\
& State-aware ABS & retrained & TODO & TODO & TODO & State-free global reference \\
\bottomrule
\end{tabular}}
\end{table*}

\subsection{Real-Robot Evaluation}
\label{sec:real_robot}
The real-robot experiments serve two purposes: validating retry under physical disturbances and testing cross-backbone transfer. We switch the backbone from StarVLA to $\pi_0$ (OpenPi)~\cite{pi0}---a diffusion-based VLA with a different architecture and pretraining corpus---and attach the identical dual-head module without architecture-specific adaptation, yielding a controlled $\pi_0$ vs.\ $\pi_0$+DUET comparison.

\paragraph{Platform and protocol.}
We deploy $\pi_0$+DUET on a Franka Emika Panda with a parallel-jaw gripper and a wrist-mounted RealSense camera. Four tasks span distinct control regimes (Fig.~\ref{fig:real_robot}): \emph{pick-and-place} (free-space grasping and transport), \emph{block pushing} (non-prehensile contact), \emph{drawer opening} (constrained contact after handle acquisition), and \emph{whiteboard wiping} (sustained surface contact). Training uses only successful kinesthetic demonstrations. The dual-head module, RVQ codebook, asymmetric augmentation, and fusion logic are identical to \S\ref{sec:method}; only the backbone producing $H$ changes.

\begin{figure*}[t]
\centering
\fbox{\parbox[c][0.22\textheight][c]{0.94\textwidth}{\centering
\textbf{Real-robot tasks and retry sequences}\\[3mm]
\textbf{Top row:} task setups --- (a) pick-and-place, (b) block pushing, (c) drawer opening, (d) whiteboard wiping.\\
Arrows indicate the direction and phase of the injected EEF displacement.\\[2mm]
\textbf{Bottom row:} representative retry sequences for each task --- pre-disturbance state $\to$ applied EEF displacement $\to$ absolute-waypoint correction $\to$ return to relative control.\\
Right-most panel: corresponding $q_t$ and $\alpha_t$ traces over time.}}
\caption{Franka real-robot evaluation. Top: four tasks spanning free-space, non-prehensile, constrained, and sustained-contact regimes. Bottom: qualitative retry sequences showing the pre-disturbance configuration, the injected displacement, the absolute-waypoint-guided correction, and the return to relative control, together with the failure score $q_t$ and fusion weight $\alpha_t$ traces.}
\label{fig:real_robot}
\end{figure*}

\paragraph{Tasks and disturbances.}
Each task is evaluated under nominal execution and a controlled lateral EEF displacement that preserves the scene state:
\begin{itemize}
    \item \textbf{Pick-and-place:} reach, grasp, and deposit. Shift before grasp acquisition.
    \item \textbf{Block pushing:} push a block into a target zone. Non-prehensile control makes the approach vector critical; a small deviation deflects the block off-target. Shift during the pre-contact approach.
    \item \textbf{Drawer opening:} acquire handle and pull. Shift during the final approach.
    \item \textbf{Whiteboard wiping:} sustained surface contact. Shift during the contact stroke.
\end{itemize}
Displacement direction and magnitude are fixed before evaluation. Injection occurs after a predefined precondition and remains within workspace and force limits. $\pi_0$ and $\pi_0$+DUET trials are interleaved in randomized blocks; we report successes/trials with Wilson 95\% CIs.

\begin{table*}[t]
\caption{Real-robot evaluation ($\pi_0$ backbone). Success reported as successes/trials (\%). $\pi_0$: base policy without dual heads; $\pi_0$+DUET: full adaptive controller. $\Delta$ is $\pi_0$+DUET minus $\pi_0$.}
\label{tab:real_robot}
\centering
\footnotesize
\setlength{\tabcolsep}{3.6pt}
\begin{tabular}{@{}l l c cc cc ccc @{}}
\toprule
& & & \multicolumn{2}{c}{Nominal} & \multicolumn{2}{c}{Disturbed} & \multicolumn{3}{c}{Retry} \\
\cmidrule(lr){4-5}\cmidrule(lr){6-7}\cmidrule(lr){8-10}
Task & Disturbance & Cov. & $\pi_0$ & +DUET & $\pi_0$ & +DUET & $\pi_0$ & +DUET & $\Delta$ \\
\midrule
Pick-and-place & EEF shift (TODO\,cm) & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Block pushing & EEF shift (TODO\,cm) & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Drawer opening & EEF shift (TODO\,cm) & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Whiteboard wiping & EEF shift (TODO\,cm) & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
\midrule
Overall & --- & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
\bottomrule
\end{tabular}
\end{table*}

Block pushing is a particularly informative case: a displaced end-effector contacts the block at the wrong point, deflecting it off-target. The base $\pi_0$ policy cannot correct this misalignment, whereas $\pi_0$+DUET re-anchors the approach via its absolute waypoint before contact. Figure~\ref{fig:real_robot} (bottom) shows representative retry sequences and the corresponding $q_t$/$\alpha_t$ traces. That the same module improves retry on a backbone never seen during simulation development confirms the transferability claim.

\subsection{Discussion}
The results support all three contributions.

\paragraph{Detection.} Cross-space disagreement leads all success-only detectors on AUROC ($0.845$). The low binary TNR reflects the continuous nature of $\alpha_t$: on clean trajectories the weight stays near zero (\S\ref{sec:ablations}), so the mild blending acts as beneficial stabilization---corroborated by the $+3.1$ pp clean improvement.

\paragraph{Retry.} The three-way comparison disentangles co-training from fusion. DUET outperforms both baselines in every aggregate, with the largest margin ($+36.7$ pp) where relative control lacks a return target. Controller traces consistently show agreement $\to$ disagreement rise $\to$ waypoint correction $\to$ smooth return.

\paragraph{Ablations.} Removing the absolute branch removes most OOD gain; absolute-only or fixed-fusion control collapses performance. The $\tau/k$ sweep confirms a broad stable region.

\paragraph{Cross-backbone transfer.} StarVLA (simulation) and $\pi_0$ (real robot) differ in architecture, pretraining, and action representation, yet the identical dual-head module yields consistent retry gains on four physical tasks (Table~\ref{tab:real_robot}) without backbone-specific tuning. This validates DUET as a general-purpose retry add-on for VLA policies.

\section{LIMITATIONS}

DUET assumes absolute state labels aligned with relative action chunks---natural in LIBERO and many robot logs but requiring accurate calibration on the Franka platform. The absolute waypoint is a demonstration anchor, not an object tracker: a world-state shift can invalidate the reference even when the robot remains nearby. On LIBERO, $S_t$ is uninformative, so gating relies on the residual to the predicted waypoint and cannot distinguish ``the arm left the target'' from ``the target moved.'' Complete visual dropout requires a validity guard and cached waypoint rather than disagreement-based detection. Retry is limited to recoverable deviations; irreversible failures or tasks requiring a new strategy fall outside the mechanism.

\section{CONCLUSION}

We presented DUET, a backbone-agnostic dual-head module for endogenous failure detection and trajectory re-entry. By aligning relative actions with stateless, RVQ-anchored absolute waypoints, DUET uses cross-space disagreement to detect when execution has left the demonstrated behavior and uses the waypoint correction to retry it. The module transfers across VLA architectures: we validated the identical design on StarVLA (simulation) and $\pi_0$/OpenPi (real robot) without architecture-specific adaptation. The controller preserves nominal performance and substantially improves completion under recoverable deviations without failure labels or takeover data. Its scope is deliberately narrower than general recovery planning; stale world references, irreversible failures, and fully invalid observations require additional mechanisms.

\section*{ACKNOWLEDGMENT}

TODO: Add acknowledgments, funding, compute resources, and any required dataset or codebase acknowledgments after the author list is finalized.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
