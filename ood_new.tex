%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% DUET ICRA 2026 draft.
% Based on the official Papercept ieeeconf template downloaded from:
% https://ras.papercept.net/conferences/support/files/ieeeconf.zip
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\documentclass[letterpaper, 10 pt, conference]{ieeeconf}

\IEEEoverridecommandlockouts
\overrideIEEEmargins

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{url}

\title{\LARGE \bf
DUET: Endogenous Failure Detection and Recovery via Dual-Action Manifold Alignment
}

\author{Anonymous Authors% <-this % stops a space
\thanks{This paper is an ICRA 2026 draft. Author names and affiliations are omitted for anonymous review.}
}

\begin{document}

\maketitle
\thispagestyle{empty}
\pagestyle{empty}

\begin{abstract}
Vision-language-action (VLA) policies map visual observations and language instructions to robot actions. With limited robot post-training data, they can drift from successful behavior during closed-loop execution. We study recoverable execution deviations, where retrying the demonstrated behavior remains feasible, rather than severe failures that require task-level replanning or a new manipulation strategy. Existing solutions often add external failure detectors and collect large amounts of human takeover data, making the resulting systems costly and loosely coupled. We introduce DUET (Dual-manifold Unified Execution and Transfer), a dual-action-head module that predicts both relative actions for smooth nominal control and absolute waypoints for trajectory re-entry. The endogenous disagreement between these action spaces detects when execution has become inconsistent with the demonstrated behavior, while the absolute waypoint-induced correction initiates a retry. Asymmetric training further makes the relative branch sensitive to deviations and the absolute branch robust to visual disturbances. We evaluate DUET in LIBERO simulation and real-world manipulation under recoverable visual, viewpoint, scene, and action disturbances. DUET preserves nominal success (\textbf{TODO}\% vs. \textbf{TODO}\% for the relative-only baseline) while achieving \textbf{TODO}\% retry success, outperforming the strongest baseline by \textbf{TODO} percentage points.
\end{abstract}

\begin{figure*}[t]
\centering
\includegraphics[width=0.96\textwidth]{figures/fig1.pdf}
\caption{Failure-aware retry with dual action spaces. Relative and absolute predictions agree during nominal execution but diverge when a recoverable deviation drives the rollout OOD, exposing an endogenous disagreement score $D_t$. Relative actions provide precise local motion, whereas absolute waypoints bring the robot back toward a demonstrated execution path for retry.}
\label{fig:teaser}
\end{figure*}

\section{INTRODUCTION}

Vision-language-action (VLA) policies adapt pretrained vision-language models to map visual observations and language instructions directly to robot actions \cite{rt2,openvla,starvla}. They are attractive because one policy can connect semantic instructions to continuous manipulation. Yet a policy trained with a small amount of robot data can leave the support of successful behavior during execution. We refer to this condition as an execution failure: the observation, robot--object configuration, or induced trajectory becomes out of distribution (OOD) with respect to the successful demonstrations. The cause may be an off-center grasp, a reachable object displacement, or a partial object slip, but it may also be external, such as an unexpected occlusion, viewpoint change, or displacement of the object or robot arm. In these cases, the policy may continue issuing locally plausible commands without recognizing that the current rollout should be retried. Detecting such deviations and returning to a demonstrated execution path can improve robustness without requiring a new task planner.

Mainstream approaches commonly introduce an external monitor, critic, verifier, or predictive model for failure detection \cite{faildetect}. When recovery is also required, they typically collect large amounts of human takeover data in which an operator intervenes and demonstrates a corrective action \cite{dagger,sirius,intervengen}. This creates substantial data and deployment cost, especially when the disturbances span visual observations, object configurations, and robot poses. More importantly, the detector only indicates that execution has gone wrong, while the recovery controller is trained as a separate capability; their decisions need not be consistent. We ask whether the policy can use its own action predictions to recognize a recoverable OOD deviation and retry the learned behavior without additional failure labels or takeover demonstrations.

DUET is motivated by a simple asymmetry that we observed in preliminary experiments. Relative-action policies produce smooth and stable motion around the demonstrated trajectory, but once a grasp or pose deviates, a local command provides no explicit target for returning to the previous behavior. Absolute-action policies are less suited to fine local control, yet their predicted waypoints can point back toward a reachable portion of the demonstrated trajectory and make a retry possible. DUET therefore learns the same successful behavior in two action spaces: a relative head for nominal execution and an absolute head for trajectory re-entry. We use the disagreement between their predictions as an endogenous signal of OOD execution. The disagreement is expected to remain small when both heads recognize the same in-distribution behavior and to increase when the current rollout is no longer compatible with that behavior.

At inference time, DUET converts the absolute waypoint into a correction from the live pose and compares it with the relative action. A soft gate keeps relative control in the nominal regime and shifts toward the waypoint-induced correction when the disagreement indicates a recoverable deviation, allowing the robot to re-approach the demonstrated behavior and retry. The gate returns to relative control after the two predictions realign. This is a failure-aware retry mechanism, not a general recovery planner: DUET does not claim to solve irreversible failures, unreachable dropped objects, hardware faults, or cases that require discovering a new task strategy.

Our contributions are threefold:
\begin{itemize}
    \item We formulate failure-aware retry as detecting recoverable OOD execution deviations and re-entering the support of successful behavior, separating this problem from severe failure recovery and task-level replanning.
    \item We propose DUET, a dual-action-head VLA that uses endogenous disagreement between relative and absolute action spaces to detect deviations and to activate an absolute waypoint-based retry while preserving smooth nominal control.
    \item We evaluate DUET in simulation and on a real robot across diverse visual, viewpoint, scene, and action disturbances, measuring both nominal robustness and retry success without additional human takeover data.
\end{itemize}


\section{RELATED WORK}

\subsection{Failure Detection for Robot Policies}
Runtime failure detection estimates whether a policy remains within the support of successful behavior. Diff-DAgger uses diffusion uncertainty to request expert intervention \cite{diffdagger}, while Sentinel monitors action inconsistency and task progress \cite{sentinel}. FAIL-Detect formulates failure detection as sequential OOD testing using only successful executions \cite{faildetect}; FIPER similarly combines representation novelty with action uncertainty without failure data \cite{fiper}.

VLA-specific methods use internal features or external foundation models. SAFE learns a detector from successful and failed rollouts \cite{safe}, whereas AHA trains a VLM to detect and diagnose failures \cite{aha}. Such methods require additional supervision or a separate detector whose output is not a recovery action. DUET instead detects failures through disagreement between two action spaces learned within the policy, using OOD manifold inconsistency as both an alarm and a recovery signal.

\subsection{Recovery from Execution Failures}
Recovery methods commonly expand the training distribution with expert corrections. DAgger queries experts on policy-induced states \cite{dagger}; intervention-based methods collect human takeovers \cite{eil,sirius}; and IntervenGen transforms limited interventions into corrective trajectories \cite{intervengen}. These approaches reduce compounding errors but still require additional recovery actions.

Foundation-model systems instead use semantic supervisors. RACER learns from language-annotated recovery trajectories \cite{racer}, while AHA and VLM controllers diagnose failures and guide downstream correction \cite{aha,vlmrecovery}. Their detection and recovery components remain separately trained or prompted. DUET couples both capabilities without extra recovery data: absolute states from successful demonstrations provide recovery anchors, while cross-space disagreement determines when and how strongly to move toward them.

\section{METHOD}

\subsection{Problem Formulation}
At time $t$, the robot observes multi-view images $o_t$, a language instruction $l$, and its current proprioceptive state $s_t$. DUET is trained only on successful demonstrations and predicts the demonstrated behavior in two action representations over a horizon $K$:
\begin{align}
    \hat{\Delta A}_t &= f_{\mathrm{rel}}(o_t,l,s_t)
    \in \mathbb{R}^{K\times 7}, \\
    \hat{W}_t &= f_{\mathrm{abs}}(o_t,l)
    \in \mathbb{R}^{K\times 7}.
\end{align}
Here, $\Delta A_t$ contains relative operational-space-control commands and $W_t$ contains future absolute end-effector waypoints $[x,y,z,r_x,r_y,r_z,g]$. We decompose each command as $a_t=[a_{p,t},a_{g,t}]$, where $a_{p,t}\in\mathbb{R}^6$ controls end-effector pose and $a_{g,t}\in\mathbb{R}$ controls the gripper. A successful trajectory naturally supplies both labels: its recorded delta actions supervise $f_{\mathrm{rel}}$, while its future end-effector states supervise $f_{\mathrm{abs}}$. The geometric analysis and fusion operate on $a_p$; although the absolute head also predicts gripper state during training, the deployed $a_g$ always comes from the relative head.

\begin{figure}[!t]
\centering
\includegraphics[width=\columnwidth]{figures/fig2.pdf}
\caption{DUET in four stages: a shared vision-language encoder, relative and absolute action heads, cross-space failure detection, and closed-loop recovery. The relative head provides state-aware local control; the explicitly state-free absolute head uses RVQ-anchored conditions to predict global waypoints. Their disagreement determines the transition between nominal execution and recovery.}
\label{fig:architecture}
\end{figure}

\subsection{Dual Action Manifolds}
For an instruction $l$, let $\mathcal{M}_l\subset SE(3)$ denote the end-effector state manifold traced by successful demonstrations, and let $T\mathcal{M}_l$ denote its tangent bundle. Under nominal execution, the relative head predicts a local motion vector
\begin{equation}
    \hat{\delta a}_{p,t} = f_{\mathrm{rel}}(o_t,l,s_t)_p
    \in T_{p_t}\mathcal{M}_l,
\end{equation}
where $p_t$ is the current end-effector pose. This tangent representation is locally smooth and expressive, but it specifies only how to move from the current chart. Once a failure displaces the robot from the demonstrated support, the extrapolated vector field contains no explicit destination to which the robot should return.

The absolute head instead predicts a future coordinate
\begin{equation}
    \hat{w}_{p,t}=f_{\mathrm{abs}}(o_t,l)_p\in\mathcal{M}_l,
\end{equation}
which acts as a visual-language-conditioned global anchor. To compare the two representations, we introduce a state-dependent transport map
\begin{equation}
    c_t=\Psi_{p_t}(\hat{w}_{p,t})\in T_{p_t}SE(3),
    \label{eq:transport_map}
\end{equation}
which converts the absolute waypoint into a local correction at the live pose. Along an in-domain trajectory, $c_t$ and $\hat{\delta a}_{p,t}$ describe the same demonstrated motion in two coordinate systems and should therefore align. When a failure drives execution OOD, the relative head extrapolates a local field while the absolute head remains anchored to a demonstrated destination, exposing a cross-space inconsistency.

This geometry couples detection and recovery without asserting a formal OOD guarantee: the norm of the inconsistency provides a scalar failure signal, whereas the transported vector $c_t$ already provides the corresponding recovery direction. DUET can therefore change both when and where the controller moves using the same pair of predictions.

\subsection{Dual-Head Architecture}
Figure~\ref{fig:architecture} shows the shared VLM and two flow-matching action heads \cite{diffusionpolicy,pi0}. The relative head attends to the full hidden sequence and explicitly receives $s_t$ through a numerical state encoder. It is therefore state-aware and serves as the primary controller during nominal execution. The absolute head receives no explicit current proprioception. When state tokens are appended to the causal VLM prompt, they are placed last and excluded from absolute-branch pooling, while the relative head can still attend to them. This separation prevents the absolute predictor from collapsing into an integration of the current state and preserves its role as a global reference.

The absolute branch pools its state-free hidden sequence $H^0$ and applies residual vector quantization (RVQ) \cite{vqvae,rvq}:
\begin{equation}
    Z_q=\operatorname{RVQ}\!\left(\operatorname{Pool}(H^0)\right).
\end{equation}
RVQ maps continuous conditions to a discrete atlas learned from successful demonstrations, anchoring absolute predictions to recurrent visual-language modes rather than an unconstrained feature vector. Its residual norm is retained only as auxiliary telemetry and is not part of the primary failure score. The absolute flow head additionally uses classifier-free guidance (CFG) \cite{cfg}, enabling its conditional prediction to be softened toward the demonstrated waypoint marginal during recovery.

\subsection{Disagreement-Preserving Learning}
The two heads are supervised by the paired labels available in every successful trajectory. Both use flow-matching objectives, while RVQ contributes a commitment loss. The complete default objective is
\begin{equation}
    \mathcal{L} =
    \lambda_{\mathrm{rel}}\mathcal{L}_{\mathrm{rel}}
    + \lambda_{\mathrm{abs}}\mathcal{L}_{\mathrm{abs}}
    + \lambda_{\mathrm{vq}}\mathcal{L}_{\mathrm{vq}},
\label{eq:training_loss}
\end{equation}
where the absolute labels are normalized future states and the relative labels are the recorded delta commands. We deliberately impose no cross-head consistency loss or distillation: forcing agreement everywhere would suppress the inconsistency needed for failure detection.

DUET instead specializes the branches through asymmetric visual augmentation. The absolute head and RVQ adapter train on both clean and perturbed observations, encouraging the global anchor to remain stable under degraded input. The relative head is optimized only on the clean subset $\mathcal{B}_{\mathrm{clean}}$:
\begin{equation}
    \mathcal{L}_{\mathrm{rel}} =
    \frac{1}{|\mathcal{B}_{\mathrm{clean}}|}
    \sum_{i \in \mathcal{B}_{\mathrm{clean}}}
    \ell_{\mathrm{FM}}(\hat{\Delta a}^{(i)}, \Delta a^{(i)}).
\end{equation}
This gradient truncation preserves relative-head sensitivity while training the absolute branch as the robust reference, making cross-space disagreement more discriminative under visual OOD. For CFG training, the absolute head also replaces its quantized condition with learned null tokens on a subset of samples and learns both conditional and marginal waypoint flows.

\subsection{Joint Failure Detection and Recovery}
At deployment, the policy returns both unnormalized action chunks. Let the live pose be $p_t=(x_t,R_t)$ and the pose component of the current absolute waypoint be $\hat{w}_{p,t}=(\hat{x}_t,\hat{R}_t)$. DUET implements the transport map in Eq.~\eqref{eq:transport_map} as
\begin{equation}
    c_t=\operatorname{clip}\!\left(
    \begin{bmatrix}
    (\hat{x}_t-x_t)/\sigma_p \\
    \operatorname{Log}(\hat{R}_tR_t^{-1})/\sigma_R
    \end{bmatrix},-1,1\right),
    \label{eq:recovery_vector}
\end{equation}
where $\operatorname{Log}:SO(3)\rightarrow\mathfrak{so}(3)$ gives the geodesic rotation correction and $\sigma_p,\sigma_R$ convert pose error into action units. The cross-space disagreement is
\begin{equation}
    D_t=\frac{\left\|c_t-
    \operatorname{clip}(\hat{\delta a}_{p,t},-1,1)\right\|_2}{\sqrt{6}}.
    \label{eq:disagreement}
\end{equation}
This is the primary failure signal. We supplement it with a coarse state-distance term that measures normalized violation of the training-state bounds:
\begin{equation}
    S_t =
    \mathrm{mean}\left[
    \frac{\max(s_{\min} - p_t, 0)}{s_{\max}-s_{\min}}
    +
    \frac{\max(p_t - s_{\max}, 0)}{s_{\max}-s_{\min}}
    \right].
\end{equation}
The resulting failure score and recovery weight are
\begin{align}
    q_t &= D_t + \lambda S_t, \\
    \alpha_t &= \sigma(k(q_t-\tau)).
\end{align}
DUET then interpolates between local execution and global recovery while retaining relative gripper control:
\begin{align}
    a_t &= [a_{p,t},a_{g,t}], \\
    a_{p,t} &= (1-\alpha_t)\hat{\delta a}_{p,t} + \alpha_t c_t, \\
    a_{g,t} &= \hat{\delta a}_{g,t},
    \label{eq:fused_action}
\end{align}
where $[\hat{\delta a}_{p,t},\hat{\delta a}_{g,t}]$ is the relative-head command. Thus relative control dominates along the demonstrated support, while a rising failure score shifts $a_p$ toward the absolute recovery direction; control returns smoothly after the two representations realign. When requesting a new chunk, the previous fusion weight also softens the absolute-head CFG scale,
\begin{equation}
    w_t = w_{\max} - (w_{\max}-w_{\min})\alpha_{t-1}.
\end{equation}
The disagreement $D_t$ therefore determines \emph{when} recovery is needed, while the vector $c_t$ determines \emph{how} to recover.

\section{EXPERIMENTS}

The experiments test the three claims in the introduction. First, activating dual-action retry should preserve nominal VLA performance. Second, the same controller should improve task completion under recoverable execution failures---action-induced trajectory drift, robot-arm displacement, object or grasp disturbances, transient visual occlusion, and contact-level scene interaction---without failure labels or takeover data. Third, the endogenous disagreement between the two action heads should discriminate failed from nominal execution. We report closed-loop success before detection: the introduction presents DUET as a coupled detect-and-retry controller, not as a standalone monitor.

\subsection{Evaluation Protocol}
We evaluate on the four LIBERO suites \cite{libero}---Spatial, Object, Goal, and LIBERO-10. Each suite contains $10$ tasks, and each method is evaluated from $50$ initial states per task, yielding $500$ episodes per suite. We train one suite-specific dual-head checkpoint for $10$k steps. \emph{DUET} executes the adaptive controller in Eq.~\eqref{eq:fused_action}, whereas \emph{DUET-Rel} fixes $\alpha_t=0$ and executes only the relative head of the same checkpoint. This within-checkpoint comparison is the primary control: architecture, training data, optimization, task, initial state, and perturbation are held fixed, leaving the inference-time use of the absolute branch as the only difference.

For OOD evaluation, perturbations are scheduled at a fixed policy step with deterministic event seeds. DUET and DUET-Rel are paired by identical $(\mathrm{task},\mathrm{initial\ state})$ keys. We report closed-loop task success and paired McNemar $p$-values. When a perturbation has a semantic precondition---in particular, a grasp drop is injected only if an object is actually held---we report injection coverage and keep uninjected episodes in the overall success rate rather than silently dropping them. Other policies' published clean scores are not used as OOD baselines: they do not share our initial states, injection times, severities, or success accounting.

Table~\ref{tab:ood_taxonomy} maps each protocol to the execution-failure types named in the introduction. We do not pool heterogeneous disturbance types, severities, or horizons into a single grand average.

\begin{table*}[t]
\caption{Execution-failure protocols and their correspondence to the introduction.}
\label{tab:ood_taxonomy}
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tabular}{llll}
\toprule
Family & Protocol & What becomes OOD & Introduction example \\
\midrule
Action execution & Action perturbation & Induced trajectory / command-to-motion map & Execution drift after locally plausible commands \\
Robot displacement & EEF teleport & Robot-arm pose & Unexpected displacement of the robot arm \\
Object / grasp & Vertical grasp drop & Robot--object configuration & Dropped object or failed hold \\
Visual occlusion & Camera blackout & Current visual observation & Transient occlusion \\
Contact / scene & External force & Robot--world interaction & External disturbance during contact \\
\bottomrule
\end{tabular}}
\end{table*}

\subsection{Nominal Execution}
Table~\ref{tab:benchmark} tests whether failure-aware retry compromises ordinary execution. DUET attains $96.9\%$ average success, matching the reported $\pi_{0.5}$ result and slightly exceeding the reported StarVLA-GR00T average. These external rows are benchmark context rather than a controlled comparison: StarVLA uses joint $30$k training, whereas DUET uses suite-specific $10$k checkpoints. The controlled comparison is DUET versus DUET-Rel. Adaptive fusion raises the average from $93.8\%$ to $96.9\%$, with gains on all four suites and the largest lifts on Goal ($+5.6$) and LIBERO-10 ($+5.8$). Dual-action control therefore does not trade away nominal task completion; the clean gains are consistent with the introduction's claim that a rollout can already leave the support of successful behavior even when no external perturbation is injected.

\begin{table}[t]
\caption{Nominal task success (\%) on LIBERO. External rows are reported results and are included only for benchmark context. StarVLA uses joint $30$k training; DUET uses suite-specific $10$k checkpoints.}
\label{tab:benchmark}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
Method & Spatial & Object & Goal & LIBERO-10 & Avg. \\
\midrule
$\pi_{0.5}$ (reported) & 98.8 & 98.2 & 98.0 & 92.4 & 96.9 \\
StarVLA-GR00T (reported) & 97.8 & 98.8 & 97.4 & 92.0 & 96.5 \\
\midrule
DUET & 98.8 & 99.4 & 97.6 & 91.8 & 96.9 \\
DUET-Rel & 98.0 & 99.2 & 92.0 & 86.0 & 93.8 \\
\bottomrule
\end{tabular}}
\end{table}

\subsection{Failure-Aware Retry under Execution Disturbances}
Table~\ref{tab:ood_overall} summarizes matched closed-loop success. In every aggregate row DUET matches or exceeds DUET-Rel. The largest gains occur precisely where the introduction predicts relative control to be insufficient: once locally plausible commands have driven the trajectory OOD, or once the arm has been displaced from the demonstrated pose.

\begin{table*}[t]
\caption{Closed-loop task success under matched execution-failure protocols. Each row uses only the stated scope.}
\label{tab:ood_overall}
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tabular}{lllrrrr}
\toprule
Family & Protocol & Scope & Pairs & DUET-Rel & DUET & $\Delta$ (pp) \\
\midrule
Action execution & Action perturbation & 4 suites, medium$+$severe & 4000 & 27.9 & 64.6 & $+36.7$ \\
Robot displacement & EEF teleport & 4 suites, medium & 2000 & 40.0 & 61.7 & $+21.7$ \\
Visual occlusion & Camera blackout & 4 suites, step 15 medium & 2000 & 83.4 & 92.2 & $+8.8$ \\
Visual occlusion & Camera blackout & 4 suites, step 23 medium$+$severe & 4000 & 73.5 & 90.8 & $+17.3$ \\
Object / grasp & Vertical grasp drop & Object, medium & 500 & 89.2 & 95.2 & $+6.0$ \\
Contact / scene & External force & 4 suites, medium$+$severe & 4000 & 92.22 & 95.95 & $+3.73$ \\
\bottomrule
\end{tabular}}
\end{table*}

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_ood_mechanism_grid.pdf}
\caption{Controller dynamics on the Object suite under (a) end-effector teleport, (b) vertical grasp drop, (c) action perturbation, and (d) camera blackout. Traces are aligned to the actual injection time and show means with pointwise bootstrap 95\% confidence intervals over successfully injected episodes. Shading denotes the perturbation or forced-hold window.}
\label{fig:ood_mechanism_grid}
\end{figure}

\begin{table}[t]
\caption{Closed-loop task success (\%) under action, robot-pose, and object/grasp disturbances. DUET and DUET-Rel use the same checkpoint. $N{=}500$ per cell.}
\label{tab:behavioral}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llcccc}
\toprule
Setting & Suite & DUET-Rel & DUET & $\Delta$ & $p$ \\
\midrule
\multicolumn{6}{l}{\textit{Action-execution shift} (action perturbation, step $5$, $N{=}500$)} \\
medium & Spatial & 54.0 & 74.0 & $+20.0$ & $1{\times}10^{-14}$ \\
medium & Object & 48.8 & 84.4 & $+35.6$ & $1{\times}10^{-34}$ \\
medium & Goal & 47.2 & 74.2 & $+27.0$ & $5{\times}10^{-22}$ \\
medium & LIBERO-10 & 36.6 & 78.6 & $+42.0$ & $1{\times}10^{-48}$ \\
severe & Spatial & 12.8 & 43.2 & $+30.4$ & $6{\times}10^{-31}$ \\
severe & Object & 7.8 & 60.6 & $+52.8$ & $1{\times}10^{-70}$ \\
severe & Goal & 10.6 & 52.6 & $+42.0$ & $3{\times}10^{-49}$ \\
severe & LIBERO-10 & 5.4 & 49.0 & $+43.6$ & $2{\times}10^{-53}$ \\
& Overall & 27.9 & \textbf{64.6} & $\mathbf{+36.7}$ & --- \\
\midrule
\multicolumn{6}{l}{\textit{Robot-arm displacement} (end-effector teleport, medium, $N{=}500$)} \\
& Spatial & 54.6 & 57.6 & $+3.0$ & 0.21 \\
& Object & 36.2 & 76.0 & $+39.8$ & $1{\times}10^{-40}$ \\
& Goal & 33.4 & 52.4 & $+19.0$ & $2{\times}10^{-12}$ \\
& LIBERO-10 & 35.8 & 60.8 & $+25.0$ & $1{\times}10^{-17}$ \\
& Overall & 40.0 & \textbf{61.7} & $\mathbf{+21.7}$ & --- \\
\midrule
\multicolumn{6}{l}{\textit{Object / grasp disturbance} (vertical grasp drop, Object, $N{=}500$)} \\
& Object & 89.2 & \textbf{95.2} & $\mathbf{+6.0}$ & $2.4{\times}10^{-5}$ \\
\bottomrule
\end{tabular}}
\end{table}

\subsubsection{Action-Execution Failures}
We first corrupt the executed 6D command over a short window. The scene is unchanged, but execution error accumulates and the induced trajectory leaves the demonstrated support---the setting in which a relative policy can keep issuing locally plausible commands without a return target. The evaluator provides no perturbation flag; any switch must come from the two action predictions.

This is the strongest closed-loop result. Over eight suite--severity groups ($4000$ paired episodes), DUET reaches $64.6\%$ success versus $27.9\%$ for DUET-Rel ($+36.7$ points; Table~\ref{tab:behavioral}). Every cell improves, and the gap widens as relative control collapses: under severe perturbations DUET-Rel falls to $5.4$--$12.8\%$, while DUET retains $43.2$--$60.6\%$, including $+52.8$ points on Object. Figure~\ref{fig:ood_mechanism_grid}(c) shows disagreement and the fusion weight rising as the arm drifts, then returning toward relative control after re-entry.

\subsubsection{Robot-Arm Displacement}
We next teleport the end effector while leaving the scene unchanged, corresponding to an unexpected arm displacement. Relative commands remain locally defined around the off-trajectory pose; the absolute head still predicts a demonstrated waypoint and supplies a retry direction.

Across $2000$ paired episodes, DUET improves success from $40.0\%$ to $61.7\%$ ($+21.7$ points; Table~\ref{tab:behavioral}). Object improves by $+39.8$ points, LIBERO-10 by $+25.0$, and Goal by $+19.0$. Among Object episodes with a successful injection, DUET completes $72.8\%$ versus $33.2\%$ for DUET-Rel. Figure~\ref{fig:teleport_mechanism} shows the intended switch-and-return: relative control before the displacement, absolute correction immediately afterward, and a return to the relative head after re-entry. The Spatial difference is smaller ($+3.0$ points, $p{=}0.21$), in part because about $12\%$ of episodes finish before the scheduled teleport.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig3_teleport_mechanism.pdf}
\caption{End-effector teleport on the Object suite. Top: a successful retry, with relative control before the displacement, absolute waypoint correction immediately afterward, and a return to relative control after re-entry. Bottom: mean disagreement $D_t$ and fusion weight $\alpha_t$ over $484$ successfully injected episodes, aligned to the injection time; bands are pointwise bootstrap 95\% confidence intervals.}
\label{fig:teleport_mechanism}
\end{figure}

\subsubsection{Object and Grasp Disturbances}
A vertical grasp drop changes the robot--object configuration rather than the arm pose: at policy step $67$, if a free-joint object is in gripper contact, the evaluator opens the gripper, keeps the object's $XY$ position, lowers it by $5$~cm, and holds the gripper open for $15$ steps. Episodes with no grasped object are marked \texttt{not\_grasped} and left unperturbed.

Object is the suite where this event is well defined. DUET succeeds in $476/500$ episodes ($95.2\%$) versus $446/500$ ($89.2\%$) for DUET-Rel ($+6.0$ points; bootstrap 95\% CI $[3.4,8.8]$; McNemar $p{=}2.4{\times}10^{-5}$). Injection occurs in $498/500$ DUET and $494/500$ DUET-Rel episodes. The absolute gain is smaller than under action or teleport disturbances because DUET-Rel already remains near $89\%$; the direction is the same as in the introduction's dropped-object example. On Goal, both methods receive the drop in only $282/500$ paired episodes, so we do not treat Goal as a suite-level object-disturbance result. Among those both-injected episodes the success rates are $23.4\%$ for DUET and $17.7\%$ for DUET-Rel.

\subsubsection{Transient Visual Occlusion}
A complete camera blackout removes the current visual observation. During the blackout, DUET holds the last reliable absolute waypoint and resumes dual-action control when vision returns, using the global action stream as short-term execution memory.

On the matched step-$15$ medium protocol, success rises from $83.4\%$ to $92.2\%$ ($+8.8$ points; Table~\ref{tab:visual}). LIBERO-10 accounts for the largest suite-level gain ($+22.4$ points); Object is already at ceiling ($97.4\%$ vs.\ $97.0\%$, $p{=}0.84$). Under the longer step-$23$ medium$+$severe protocol the gap widens to $+17.3$ points ($90.8\%$ vs.\ $73.5\%$). A longer visual outage therefore increases, rather than removes, the value of the absolute waypoint. Figures~\ref{fig:ood_mechanism_grid}(d) and~\ref{fig:camera_blackout_mechanism} show that the cached waypoint is used during the blackout and that adaptive control resumes afterward. The Goal task ``open the top drawer and put the bowl inside'' remains a counterexample at step $15$: a contact sequence that still needs updated visual feedback is outside the retry mechanism claimed in the introduction.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/fig_camera_blackout_mechanism.pdf}
\caption{Camera blackout on the Object suite. DUET holds the last reliable absolute waypoint while the image is invalid and resumes adaptive dual-action control when vision returns. The lower panel aggregates $500$ episodes aligned to blackout onset.}
\label{fig:camera_blackout_mechanism}
\end{figure}

\begin{table}[t]
\caption{Task success (\%) under camera blackout. Step-$15$ medium uses $2000$ paired episodes; step-$23$ aggregates eight suite--severity groups.}
\label{tab:visual}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llcccc}
\toprule
Protocol & Suite & DUET-Rel & DUET & $\Delta$ & $p$ \\
\midrule
\multicolumn{6}{l}{\textit{Step $15$, medium} ($N{=}500$)} \\
& Spatial & 91.2 & 96.8 & $+5.6$ & $3{\times}10^{-5}$ \\
& Object & 97.4 & 97.0 & $-0.4$ & 0.84 \\
& Goal & 80.8 & 88.2 & $+7.4$ & $5{\times}10^{-4}$ \\
& LIBERO-10 & 64.2 & 86.6 & $+22.4$ & $1{\times}10^{-17}$ \\
& Overall & 83.4 & \textbf{92.2} & $\mathbf{+8.8}$ & --- \\
\midrule
\multicolumn{6}{l}{\textit{Step $23$, medium$+$severe} ($N{=}4000$)} \\
& Overall & 73.5 & \textbf{90.8} & $\mathbf{+17.3}$ & --- \\
\bottomrule
\end{tabular}}
\end{table}

\subsubsection{Contact and Scene Interaction}
We apply an external force during contact, perturbing robot--world dynamics without resetting the end-effector pose. Across eight suite--severity groups, DUET reaches $95.95\%$ versus $92.22\%$ for DUET-Rel ($+3.73$ points; Table~\ref{tab:force}). Every cell is non-negative. The largest lifts are on Goal ($+7.6$ and $+9.6$ points), where drawer and plate interactions amplify short contact disturbances. Relative control is already strong on Spatial and Object ($>96\%$), so the aggregate margin is smaller than under action or teleport failures; the result still shows that dual-action retry does not destabilize contact and helps on the more interaction-rich suite.

\begin{table}[t]
\caption{Closed-loop success (\%) under external-force contact disturbances (fixed-step, $100\%$ injection, $N{=}500$ per cell).}
    \label{tab:force}
    \centering
    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{lcccccc}
    \toprule
    & \multicolumn{3}{c}{Medium} & \multicolumn{3}{c}{Severe} \\
    \cmidrule(lr){2-4}\cmidrule(lr){5-7}
    Suite & DUET-Rel & DUET & $\Delta$ & DUET-Rel & DUET & $\Delta$ \\
    \midrule
    Spatial & 96.6 & 98.4 & $+1.8$ & 96.8 & 96.8 & $0.0$ \\
    Object & 97.6 & 99.0 & $+1.4$ & 96.2 & 96.8 & $+0.6$ \\
    Goal & 89.2 & 96.8 & $+7.6$ & 87.2 & 96.8 & $+9.6$ \\
    LIBERO-10 & 87.8 & 92.6 & $+4.8$ & 86.4 & 90.4 & $+4.0$ \\
    \midrule
    Overall & \multicolumn{6}{c}{DUET-Rel $92.22$ \quad DUET $95.95$ \quad $\Delta=+3.73$} \\
    \bottomrule
    \end{tabular}}
    \end{table}

\subsection{Endogenous Failure Detection}
The introduction uses disagreement as an endogenous OOD signal that is coupled to control, not as a hard alarm trained on failure labels. We therefore compare DUET's runtime score with detectors that likewise require no failure examples: logpZO, RND, and PCA-kmeans from the FAIL-Detect framework \cite{faildetect}, and STAC, which scores temporal inconsistency between overlapping action chunks \cite{sentinel}. Table~\ref{tab:detector_baselines} reports discrimination and temporal localization under the same protocol.

\begin{table*}[t]
\caption{Failure-detection comparison. TPR, TNR, balanced accuracy (Bal.-Acc.), AUROC, and Loc.@16 are higher-is-better; detection delay is lower-is-better. Bold values indicate the best result in each column.}
\label{tab:detector_baselines}
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tabular}{lccccccc}
\toprule
Method & TPR $\uparrow$ & TNR $\uparrow$ & Bal.-Acc. $\uparrow$ & AUROC $\uparrow$ & Delay (steps) $\downarrow$ & Delay (s) $\downarrow$ & Loc.@16 $\uparrow$ \\
\midrule
DUET & \textbf{0.900} & 0.265 & 0.582 & \textbf{0.845} & 38.3 & 1.91 & 0.523 \\
logpZO~\cite{faildetect} & 0.889 & 0.243 & 0.566 & 0.817 & 22.7 & 1.14 & \textbf{0.569} \\
RND~\cite{rnd,faildetect} & 0.842 & \textbf{0.361} & \textbf{0.602} & 0.549 & 26.1 & 1.31 & 0.330 \\
PCA-kmeans~\cite{faildetect} & 0.884 & 0.283 & 0.583 & 0.625 & \textbf{20.3} & \textbf{1.01} & 0.503 \\
STAC~\cite{sentinel} & 0.839 & 0.311 & 0.575 & 0.494 & 25.8 & 1.29 & 0.260 \\
\bottomrule
\end{tabular}}
\end{table*}

DUET attains the highest TPR ($0.900$) and AUROC ($0.845$), so the dual-action score ranks failed versus nominal execution more strongly than the dedicated baselines. Because the score is used for soft fusion rather than a binary interrupt, we treat ranking and sensitivity as the relevant detection properties. RND has a higher TNR and PCA-kmeans a shorter delay; those operating points matter for a hard monitor, but they are not the objective stated in the introduction.

As a complementary analysis, Table~\ref{tab:detection} will compare score variants on frozen DUET-Rel trajectories, so that a successful correction cannot shorten its own positive example. Positive windows begin at the known injection time on action-perturbation and teleport rollouts; negatives are time-matched clean windows. Thresholds are calibrated on a held-out clean split. Values will be filled after this analysis is complete.

\begin{table*}[t]
\caption{Failure detection on frozen DUET-Rel trajectories. Recall is measured at a threshold calibrated to $1\%$ clean-window FPR; delay is event-level, and false alarms are measured over full clean rollouts. Values are placeholders until analysis is complete.}
\label{tab:detection}
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tabular}{lccccccccc}
\toprule
& \multicolumn{4}{c}{End-effector teleport} & \multicolumn{4}{c}{Action perturbation} & Clean \\
\cmidrule(lr){2-5}\cmidrule(lr){6-9}
Detection signal & AUROC & AUPRC & Recall & Delay & AUROC & AUPRC & Recall & Delay & FA/1k \\
\midrule
State-bound violation $S_t$ & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
RVQ residual & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Cross-action disagreement $D_t$ & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Residual-weighted $D_t r_t$ & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Default score $D_t+\lambda S_t$ & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
\bottomrule
\end{tabular}}
\end{table*}

\subsection{Ablations}
The introduction attributes retry to two complementary action streams and to disagreement-driven switching, not to a second head that is always on or mixed at a constant weight. Table~\ref{tab:ablations} therefore separates inference-time controls on the shared dual-head checkpoint from training-time replacements that require a new run. We report clean success together with the two disturbances where relative control loses a return target---robot-arm teleport and action-execution corruption.

Disabling the absolute branch (DUET-Rel, $\alpha_t=0$) is the completed inference ablation. It accounts for the closed-loop gains already reported: clean success falls from $96.9\%$ to $93.8\%$, teleport from $61.7\%$ to $40.0\%$, and action-execution from $64.6\%$ to $27.9\%$. Holding the absolute CFG scale fixed at $w{=}1$ on Object leaves performance essentially unchanged ($98.2\%$ clean vs.\ $99.4\%$; $83.2\%$ vs.\ $84.4\%$ under action-medium). The retry benefit therefore comes from using the absolute waypoint, not from adaptive guidance scaling. Absolute-only control ($\alpha_t=1$) and fixed fusion ($\alpha_t=0.5$) reuse the same checkpoint and will test whether a constant mix can replace the disagreement gate; those runs are not yet complete.

Training ablations need matched $10$k checkpoints. Replacing RVQ with an MLP tests discrete waypoint anchoring; training the relative head on perturbed images tests whether asymmetric augmentation is needed to keep the two streams complementary; and giving the absolute head proprioception tests the state-free global reference. These rows are preregistered and left as TODO.





\begin{table*}[t]
\caption{Component ablations, success (\%). Full DUET and DUET-Rel are four-suite averages (clean $N{=}2000$; robot-state teleport medium $N{=}2000$; action-execution medium$+$severe $N{=}4000$). Entries marked $^\dagger$ are Object-only ($N{=}500$ per protocol; Tables~\ref{tab:abl_abs_only}--\ref{tab:abl_nocfg}). Matched Object Full-DUET rates are $99.2\%$ clean, $76.0\%$ teleport (step~$39$), and $84.4\%$ action-execution medium (step~$5$).}
\label{tab:ablations}
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tabular}{lllcccl}
\toprule
Type & Variant & Checkpoint & Clean & Robot-state & Action-execution & Isolates \\
\midrule
\multirow{5}{*}{Inference}
& Full DUET & shared & 96.9 & 61.7 & 64.6 & Complete controller \\
& DUET-Rel ($\alpha=0$) & shared & 93.8 & 40.0 & 27.9 & Absolute retry branch \\
& Absolute-only ($\alpha=1$)$^\dagger$ & shared & 5.2 & 1.1 & 0.8 & Cost of losing local control \\
& Fixed fusion ($\alpha=0.5$)$^\dagger$ & shared & 46.2 & 39.4 & 41.0 & Adaptive switching \\
& No adaptive CFG$^\dagger$ & shared & 88.2 & 52.6 & 57.2 & CFG scale schedule \\
\midrule
\multirow{3}{*}{Training}
& RVQ $\rightarrow$ MLP & retrained & TODO & TODO & TODO & Discrete waypoint anchoring \\
& Symmetric augmentation & retrained & TODO & TODO & TODO & Complementary specialization \\
& State-aware ABS & retrained & TODO & TODO & TODO & State-free global reference \\
\bottomrule
\end{tabular}}
\end{table*}

\subsection{Real-Robot Evaluation}
\paragraph{Platform and protocol.}
We deploy DUET on a Franka manipulator and evaluate three tasks with distinct control requirements (Fig.~\ref{fig:real_robot_setup}). \emph{Pick-and-place} requires free-space reaching, grasping, and object transport; \emph{drawer opening} requires precise handle acquisition followed by constrained contact motion; and \emph{whiteboard wiping} requires sustained surface contact while tracking an extended path. The final manuscript will additionally report the Franka model, end-effector, camera placement, control frequency, number of demonstrations per task, training budget, and end-to-end inference latency. As in simulation, training uses only successful demonstrations, without failure labels or human takeover trajectories.

\begin{figure*}[t]
\centering
\begin{tabular}{ccc}
\fbox{\parbox[c][0.115\textheight][c]{0.29\textwidth}{\centering
\textbf{(a) Pick-and-place}\\[2mm]
Replace with task-scene image}} &
\fbox{\parbox[c][0.115\textheight][c]{0.29\textwidth}{\centering
\textbf{(b) Drawer opening}\\[2mm]
Replace with task-scene image}} &
\fbox{\parbox[c][0.115\textheight][c]{0.29\textwidth}{\centering
\textbf{(c) Whiteboard wiping}\\[2mm]
Replace with task-scene image}}
\end{tabular}
\caption{Reserved layout for the Franka real-robot setup and three evaluation scenes: (a) pick-and-place, (b) drawer opening, and (c) whiteboard wiping. The final panels should mark the disturbance direction and injection phase so that the recoverability assumption is visually explicit.}
\label{fig:real_robot_setup}
\end{figure*}

For each task, we evaluate nominal execution and a controlled end-effector displacement that leaves the scene and task objective unchanged. The displacement is injected before grasp acquisition for pick-and-place, during the final handle approach for drawer opening, and during the contact stroke for whiteboard wiping. This common intervention isolates trajectory re-entry while exposing the controller to free-space, constrained, and sustained-contact regimes. The task-specific displacement direction and magnitude are fixed before evaluation and measured from Franka forward kinematics rather than estimated from video.

Injection occurs only after a predefined visual or contact precondition and must remain within prespecified workspace and force limits. A trial is marked as injected when these criteria are satisfied at intervention time; no trial is excluded based on its subsequent outcome. Trials that never reach the precondition are reported separately as uninjected rather than counted as retry failures. Scene resets use physical markers or fixtures, and DUET and DUET-Rel trials are interleaved in randomized blocks to reduce confounding from hardware drift. Outcomes are scored using predefined task criteria:
\begin{itemize}
    \item \textbf{Pick-and-place:} the commanded object is lifted and deposited fully inside the target receptacle.
    \item \textbf{Drawer opening:} the robot acquires the correct handle and moves the drawer beyond a prespecified displacement threshold.
    \item \textbf{Whiteboard wiping:} the robot maintains safe contact and removes at least a prespecified fraction of a standardized marked region.
\end{itemize}
In addition to overall task success, we report injection coverage and \emph{retry success}: completion among trials satisfying the prespecified injection criteria. We report successes/trials with Wilson 95\% confidence intervals rather than hiding the small physical-trial denominator behind percentages. A hardware safety stop is always active but is not used as DUET's failure signal; safety-stop events are reported as failures.

\begin{table*}[t]
\caption{Real-robot evaluation. Success entries are reported as successes/trials (\%); retry success conditions only on trials meeting the prespecified injection criteria. Values and disturbance definitions are placeholders pending physical experiments.}
\label{tab:real_robot}
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tabular}{lllccccccc}
\toprule
Task & Recoverable disturbance & Coverage & \multicolumn{2}{c}{Nominal success} & \multicolumn{2}{c}{Disturbed success} & \multicolumn{3}{c}{Retry success} \\
\cmidrule(lr){4-5}\cmidrule(lr){6-7}\cmidrule(lr){8-10}
& & & DUET-Rel & DUET & DUET-Rel & DUET & DUET-Rel & DUET & $\Delta$ \\
\midrule
Pick-and-place & EEF shift before grasp (TODO cm) & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Drawer opening & EEF shift before contact (TODO cm) & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
Whiteboard wiping & EEF shift during stroke (TODO cm) & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
\midrule
Overall & --- & TODO & TODO & TODO & TODO & TODO & TODO & TODO & TODO \\
\bottomrule
\end{tabular}}
\end{table*}

% TODO: Add a real-robot qualitative figure showing, for each task, the
% pre-disturbance state, applied deviation, absolute-waypoint retry, and
% return to relative control, together with the corresponding q_t/alpha_t trace.

\subsection{Discussion}
The tables support the introduction's three claims. Dual-action control preserves nominal LIBERO success ($96.9\%$ vs.\ $93.8\%$ for DUET-Rel) and matches the reported $\pi_{0.5}$ average, so retry is not purchased by sacrificing the relative controller. Under recoverable execution failures, the same checkpoint comparison is uniformly non-negative and is largest where relative actions lose a return target: $+36.7$ points after action-induced trajectory drift and $+21.7$ points after robot-arm displacement. Object/grasp drops ($+6.0$), transient camera blackouts ($+8.8$ and $+17.3$), and contact forces ($+3.73$) move in the same direction. The controller traces show the mechanism stated in the introduction: the two heads agree in the nominal regime, disagreement rises after a disturbance, the absolute waypoint supplies a retry direction, and relative control resumes after re-entry.

The detection comparison is consistent with that coupled-controller view. Disagreement attains the highest TPR and AUROC among detectors trained without failure examples. We do not interpret TNR or delay as the primary detection claim, because the score modulates a soft gate rather than firing a standalone alarm. The completed ablation in Table~\ref{tab:ablations} likewise matches the introduction: removing the absolute branch removes most of the OOD gain, whereas freezing CFG does not. Absolute-only control, fixed fusion, and the training replacements remain to be run. Real-robot numbers in Table~\ref{tab:real_robot} remain to be filled; the protocol tests the same retry behavior on free-space, constrained, and sustained-contact tasks without takeover data. As stated in the introduction, DUET is a failure-aware retry mechanism, not a planner for irreversible failures or a new task strategy.

\section{LIMITATIONS}

DUET assumes access to absolute state labels aligned with the relative action chunks. This is natural in LIBERO and many robot logs, but requires accurate calibration and state estimation on the Franka platform. The absolute waypoint is an RVQ demonstration anchor, not an object tracker: a world-state shift can invalidate the grasp reference even when the robot remains nearby. The bound-violation term $S_t$ is uninformative on LIBERO, so robot-state gating relies on the residual to the predicted waypoint; this still cannot distinguish ``the arm left a valid target'' from ``the target itself moved.'' Complete visual dropout requires an explicit validity guard and a cached waypoint rather than disagreement-based detection. Matched component ablations, detection analysis, and Franka results remain incomplete in this draft.

\section{CONCLUSION}

We presented DUET, a dual-branch VLA policy for endogenous failure detection and trajectory re-entry. By aligning local relative actions with stateless, RVQ-anchored absolute waypoints, DUET uses cross-space disagreement to determine when execution has left learned behavior and uses the waypoint correction to retry it. The resulting controller preserves nominal performance and substantially improves completion under recoverable pose and action-execution deviations without failure labels or takeover demonstrations. Its scope is deliberately narrower than general recovery planning: stale world references, irreversible failures, and fully invalid observations require additional mechanisms or a new task strategy.

\section*{ACKNOWLEDGMENT}

TODO: Add acknowledgments, funding, compute resources, and any required dataset or codebase acknowledgments after the author list is finalized.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
